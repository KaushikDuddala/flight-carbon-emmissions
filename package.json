{
  "name": "flight-carbon-emmissions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
